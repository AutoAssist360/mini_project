# Tests

This folder contains all unit and integration tests for backend and frontend.

## Guidelines
- Keep tests organized mirroring `backend/` and `frontend/` structure
- Use meaningful test names
- Run tests before opening a Pull Request
- Include edge cases and error scenarios

## Running Tests
- Backend:
```bash
cd backend
npm test
```
- Frontend:
```bash
cd frontend
npm test
```